# Configuration Vision LLM API

# === Workers Configuration ===
# Active/désactive les workers Bun pour améliorer les performances
EK_ENABLE_WORKERS=true

# Nombre de workers pour extraction PDF (défaut: 2)
EK_PDF_WORKERS=2

# Nombre de workers pour optimisation Vision (défaut: 3) 
EK_VISION_WORKERS=3

# Timeout des workers en ms
EK_PDF_WORKER_TIMEOUT=60000
EK_VISION_WORKER_TIMEOUT=30000

# === API Configuration ===
# Port du serveur (défaut: 3001)
PORT=3001

# === AI Providers ===
# Scaleway AI
EK_AI_API_KEY=your_scaleway_api_key
EK_AI_BASE_URL=https://api.scaleway.ai/v1
# TEST_SCALEWAY_KEY=your_test_scaleway_key
# === Performance Notes ===
# Mode Workers (ENABLE_WORKERS=true):
# - Traitement parallèle PDF + Sharp
# - Recommandé pour production et gros volumes
# - Utilise plus de RAM mais bien plus rapide
#
# Mode Direct (ENABLE_WORKERS=false):
# - Traitement séquentiel classique
# - Recommandé pour dev/test ou petits volumes
# - Moins de RAM mais plus lent